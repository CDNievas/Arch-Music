{
  "name": "arch-music-master",
  "version": "1.0.0",
  "description": "Arch Music Master Coordinator",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd Slave && npm install && cd ../ && node app.js slaves.json"
  },
  "author": "CDNievas",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.2.0"
  }
}
